{% extends 'admin/admin.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1 class="w3-xxxlarge w3-text-red">Tag</h1>
    <hr>

    <table class="w3-table w3-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tag.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tag.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_tag_index') }}" class="w3-btn w3-red">Back to list</a>

    <a href="{{ path('app_tag_edit', {'id': tag.id}) }}" class="w3-btn w3-red">Edit</a>

    {{ include('admin/tag/_delete_form.html.twig') }}
{% endblock %}

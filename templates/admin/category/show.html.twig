{% extends 'admin/admin.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1 class="w3-xxxlarge w3-text-red">Category</h1>
    <hr>

    <table class="w3-table w3-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_index') }}" class="w3-btn w3-red">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}" class="w3-btn w3-red">edit</a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}

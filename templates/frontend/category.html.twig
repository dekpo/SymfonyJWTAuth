{% extends 'base.html.twig' %}

{% block title %}Hello FrontendController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>{{ category.name }}!</h1>
    <table class="w3-table w3-striped">
    {% for post in posts %}
    <tr>
    <td>{{ post.id }}</td>
                <td>{{ post.featured ? 'Yes' : 'No' }}</td>
                <td>{{ post.thumbnail }}</td>
                <td>{{ post.title }}</td>
                <td>{{ post.description }}</td>
                <td>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ post.updatedAt ? post.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
    </tr>
    {% endfor %}
    </table>
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/html/symfo-project/src/Controller/FrontendController.php'|file_link(0) }}">src/Controller/FrontendController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/html/symfo-project/templates/frontend/index.html.twig'|file_link(0) }}">templates/frontend/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
